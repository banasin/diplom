# План реализации: Часть 5 (усиление нейроподхода на задаче спектра)

> **Для исполнителя:** используйте `superpowers:subagent-driven-development`
> (рекомендуется) или `superpowers:executing-plans`. Шаги — чекбоксы `- [ ]`.

**Goal:** проверить, сокращают ли (а) ранговая цель обучения и (б) предобученный
нуклеотидный энкодер аптамера разрыв нейроподхода с интерпретируемыми моделями на
задаче спектра — абляция 2×2, честная оценка (H9).

**Architecture:** белковые пары → кластерный сплит → аптамер {k-меры | Nucleotide
Transformer} × цель {регрессия | ранжирование} на общей two-tower скор-модели →
профили test-аптамеров по панели белков → per-aptamer ROC-AUC + per-target AUC →
сравнение ячеек ↔ baseline ↔ OLMPASS IAP.

**Tech Stack:** Python 3.10, PyTorch (CUDA), transformers (Nucleotide Transformer),
ESM-2 (кэш), numpy/pandas/scikit-learn, pytest.

## Global Constraints

- Windows, GPU RTX 4070 (CUDA). Интерпретатор `.venv/Scripts/python.exe`.
- **Никаких упоминаний внешних инструментов/ассистентов** в коде, комментариях и
  сообщениях коммитов. Коммиты **без** trailer-а `Co-Authored-By`. Автор — только
  пользователь.
- Данные/веса/эмбеддинги/результаты — **вне git**. `best_models/`, `Article.docx`,
  описания OLMPASS — **вне git** (уже в `.gitignore`).
- Белковые пары: медиана log-Kd, оговорённые оставлены. Кластеризация по аптамеру
  (U→T), сплит без утечки. Честность: обучение (и негативы) — на train; профили и
  метрики — на test-аптамерах. Детерминизм при seed.
- Тесты из корня репозитория: `python -m pytest`.

---

## Структура файлов

| Файл | Ответственность |
|---|---|
| `configs/part5.yaml` | параметры Части 5 |
| `src/config.py` (расширение) | `Part5Config` + `load_part5_config` |
| `src/features/aptamer_lm.py` | эмбеддинги Nucleotide Transformer (кэш) |
| `src/models/ranking_twotower.py` | скор-two-tower + train_regression + train_ranking |
| `src/run_part5.py` | оркестратор абляции 2×2 |
| `tests/…` | юнит-тесты |

Переиспускаем: `aptamer.py`, `protein.py`/`protein_seqs.py`, `cluster.py`,
`split.py`, `data/spectrum.py`, `evaluation/ranking.py`, `evaluation/per_target.py`,
`data/olmpass.py`, `metrics.py`.

---

## Task 0: Окружение (einops) и конфиг Части 5

**Files:** Modify `requirements.txt`, `src/config.py`; Create `configs/part5.yaml`,
`tests/test_config_part5.py`.

**Interfaces:** `load_part5_config(path="configs/part5.yaml") -> Part5Config` с
полями `seed, identity_threshold, test_size, kmer_k, esm_model, nt_model,
n_negatives, ranking_loss, best_models_dir, nn`.

- [ ] **Step 1: Дописать в `requirements.txt`**

```
# Часть 5: нуклеотидная языковая модель (Nucleotide Transformer)
einops
```

- [ ] **Step 2: Установить**

Run: `.venv/Scripts/python.exe -m pip install -r requirements.txt`

- [ ] **Step 3: Тест**

Create `tests/test_config_part5.py`:
```python
from src.config import load_part5_config


def test_load_part5_config_defaults():
    cfg = load_part5_config("configs/part5.yaml")
    assert cfg.seed == 42
    assert cfg.n_negatives == 5
    assert cfg.ranking_loss == "bpr"
    assert cfg.nt_model.startswith("InstaDeepAI/")
    for key in ["aptamer_hidden", "protein_hidden", "embed_dim", "head_hidden",
                "dropout", "lr", "batch_size", "max_epochs", "patience",
                "val_fraction", "margin"]:
        assert key in cfg.nn


def test_einops_importable():
    import einops  # noqa: F401
```

- [ ] **Step 4: Запустить — упадёт**

Run: `.venv/Scripts/python.exe -m pytest tests/test_config_part5.py -v`
Expected: FAIL.

- [ ] **Step 5: Конфиг**

Create `configs/part5.yaml`:
```yaml
seed: 42
identity_threshold: 0.95
test_size: 0.2
kmer_k: 4
esm_model: esm2_t33_650M_UR50D
nt_model: InstaDeepAI/nucleotide-transformer-v2-100m-multi-species
n_negatives: 5
ranking_loss: bpr
best_models_dir: best_models
nn:
  aptamer_hidden: 256
  protein_hidden: 256
  embed_dim: 128
  head_hidden: 128
  dropout: 0.2
  lr: 0.001
  batch_size: 64
  max_epochs: 150
  patience: 20
  val_fraction: 0.15
  margin: 1.0
```

- [ ] **Step 6: Расширить `src/config.py`**

Добавить в конец:
```python
@dataclass
class Part5Config:
    seed: int
    identity_threshold: float
    test_size: float
    kmer_k: int
    esm_model: str
    nt_model: str
    n_negatives: int
    ranking_loss: str
    best_models_dir: str
    nn: dict


def load_part5_config(path: str = "configs/part5.yaml") -> Part5Config:
    data = yaml.safe_load(Path(path).read_text(encoding="utf-8"))
    return Part5Config(
        seed=int(data["seed"]),
        identity_threshold=float(data["identity_threshold"]),
        test_size=float(data["test_size"]),
        kmer_k=int(data["kmer_k"]),
        esm_model=str(data["esm_model"]),
        nt_model=str(data["nt_model"]),
        n_negatives=int(data["n_negatives"]),
        ranking_loss=str(data["ranking_loss"]),
        best_models_dir=str(data["best_models_dir"]),
        nn=dict(data["nn"]),
    )
```

- [ ] **Step 7: Запустить — пройдёт**

Run: `.venv/Scripts/python.exe -m pytest tests/test_config_part5.py -v`
Expected: 2 passed. Перед коммитом прогнать весь набор (без регрессий Частей 1–4).

- [ ] **Step 8: Commit**

```bash
git add requirements.txt configs/part5.yaml src/config.py tests/test_config_part5.py
git commit -m "Окружение (einops) и конфиг Части 5"
```

---

## Task 1: Эмбеддинги Nucleotide Transformer

**Files:** Create `src/features/aptamer_lm.py`, `tests/test_aptamer_lm.py`.

**Interfaces:** `embed_aptamers(seqs:dict[str,str], model_name:str, cache_path:str,
batch_size:int=8) -> dict[str,np.ndarray]` — mean-pooled эмбеддинг Nucleotide
Transformer, U→T, кэш `.npz` (вне git), атомарная запись, эмбеддинг раз на аптамер.

- [ ] **Step 1: Тест (крошечная NT-модель 50M)**

Create `tests/test_aptamer_lm.py`:
```python
import numpy as np
from src.features.aptamer_lm import embed_aptamers

M = "InstaDeepAI/nucleotide-transformer-v2-50m-multi-species"


def test_embed_aptamers_shape_and_cache(tmp_path):
    seqs = {"ACGTACGTAA": "ACGTACGTAA", "TTTTGGGGCC": "TTTTGGGGCC"}
    cache = tmp_path / "apt.npz"
    out = embed_aptamers(seqs, M, str(cache), batch_size=2)
    assert set(out) == set(seqs)
    H = out["ACGTACGTAA"].shape[0]
    assert out["ACGTACGTAA"].shape == (H,)
    out2 = embed_aptamers(seqs, M, str(cache))            # из кэша
    assert np.allclose(out["ACGTACGTAA"], out2["ACGTACGTAA"])


def test_u_equals_t(tmp_path):
    a = embed_aptamers({"x": "ACGUACGU"}, M, str(tmp_path / "a.npz"))
    b = embed_aptamers({"x": "ACGTACGT"}, M, str(tmp_path / "b.npz"))
    assert np.allclose(a["x"], b["x"])
```

- [ ] **Step 2: Запустить — упадёт**

Run: `.venv/Scripts/python.exe -m pytest tests/test_aptamer_lm.py -v`
Expected: FAIL.

- [ ] **Step 3: Реализовать**

Create `src/features/aptamer_lm.py`:
```python
"""Эмбеддинги аптамеров предобученной нуклеотидной языковой моделью (Nucleotide
Transformer): mean-pooling представлений, кэш .npz (вне git). U≡T (Часть 5)."""
import os
from pathlib import Path

import numpy as np
import torch
from transformers import AutoModel, AutoTokenizer


def _norm(s: str) -> str:
    return s.strip().upper().replace("U", "T")


def _atomic_savez(cache_file: Path, **arrays) -> None:
    cache_file.parent.mkdir(parents=True, exist_ok=True)
    tmp = cache_file.with_name(cache_file.name + ".tmp")
    with open(tmp, "wb") as f:
        np.savez(f, **arrays)
    os.replace(tmp, cache_file)


def embed_aptamers(seqs: dict, model_name: str, cache_path: str,
                   batch_size: int = 8) -> dict:
    cache_file = Path(cache_path)
    cached: dict = {}
    if cache_file.exists():
        with np.load(cache_file, allow_pickle=False) as npz:
            cached = {k: np.array(npz[k]) for k in npz.files}

    todo = {k: _norm(v) for k, v in seqs.items() if k not in cached}
    if todo:
        device = "cuda" if torch.cuda.is_available() else "cpu"
        tok = AutoTokenizer.from_pretrained(model_name, trust_remote_code=True)
        model = AutoModel.from_pretrained(
            model_name, trust_remote_code=True).eval().to(device)
        items = list(todo.items())
        for i in range(0, len(items), batch_size):
            chunk = items[i:i + batch_size]
            enc = tok([v for _, v in chunk], return_tensors="pt",
                      padding=True).to(device)
            with torch.no_grad():
                out = model(**enc)
            hs = getattr(out, "last_hidden_state", None)
            if hs is None:
                hs = out[0]
            mask = enc["attention_mask"].unsqueeze(-1).to(hs.dtype)
            pooled = (hs * mask).sum(1) / mask.sum(1).clamp(min=1.0)
            for j, (k, _) in enumerate(chunk):
                cached[k] = pooled[j].cpu().numpy().astype(np.float32)
        _atomic_savez(cache_file, **cached)

    return {k: cached[k] for k in seqs if k in cached}
```

- [ ] **Step 4: Запустить — пройдёт (скачает NT-50M однократно)**

Run: `.venv/Scripts/python.exe -m pytest tests/test_aptamer_lm.py -v`
Expected: PASS. Если загрузка требует доп. пакета (напр. `einops`) — он уже в
requirements (Task 0); если модель требует ещё зависимость — доустановить и
дописать в requirements, отметив в отчёте.

- [ ] **Step 5: Commit**

```bash
git add src/features/aptamer_lm.py tests/test_aptamer_lm.py
git commit -m "Эмбеддинги аптамеров Nucleotide Transformer (mean-pool, кэш)"
```

---

## Task 2: Скор-two-tower и обучение регрессией

**Files:** Create `src/models/ranking_twotower.py`, `tests/test_ranking_twotower.py`.

**Interfaces:**
- `ScoreTwoTower(nn.Module)` — `(apt_dim, prot_dim, cfg)`; `forward(x_apt, x_prot)
  -> (B,)` скаляр-скор.
- `train_regression(X_apt, X_prot, y, cfg, seed) -> tuple[ScoreTwoTower, dict]` —
  MSE по стандартизованному log-Kd; stats = {"y_mean","y_std"}.
- `predict_scores(model, X_apt, X_prot) -> np.ndarray` — сырой скор (forward).
- `ALPHABET`/утилиты по необходимости.

- [ ] **Step 1: Тест**

Create `tests/test_ranking_twotower.py`:
```python
import numpy as np
from src.models.ranking_twotower import (ScoreTwoTower, train_regression,
                                         predict_scores)

CFG = {"aptamer_hidden": 32, "protein_hidden": 32, "embed_dim": 16,
       "head_hidden": 16, "dropout": 0.0, "lr": 0.01, "batch_size": 16,
       "max_epochs": 300, "patience": 300, "val_fraction": 0.25, "margin": 1.0}


def test_regression_learns_and_shapes():
    rng = np.random.default_rng(0)
    Xa = rng.normal(size=(40, 6)).astype("float32")
    Xp = rng.normal(size=(40, 5)).astype("float32")
    y = (Xa[:, 0] + Xp[:, 0]).astype("float32")           # log-Kd
    model, stats = train_regression(Xa, Xp, y, CFG, seed=42)
    s = predict_scores(model, Xa, Xp)
    assert s.shape == (40,)
    # forward предсказывает стандартизованный log-Kd -> сила = −s должна коррелировать с −y
    corr = np.corrcoef(-s, -y)[0, 1]
    assert corr > 0.8
```

- [ ] **Step 2: Запустить — упадёт**

Run: `.venv/Scripts/python.exe -m pytest tests/test_ranking_twotower.py -v`
Expected: FAIL.

- [ ] **Step 3: Реализовать**

Create `src/models/ranking_twotower.py`:
```python
"""Two-tower скор-модель силы связывания аптамер–белок и её обучение регрессией
(Часть 5). Ранговое обучение — в этом же модуле (train_ranking, ниже по задаче)."""
import numpy as np
import torch
from torch import nn


def _seed_everything(seed: int) -> None:
    np.random.seed(seed)
    torch.manual_seed(seed)
    if torch.cuda.is_available():
        torch.cuda.manual_seed_all(seed)


def _tower(in_dim: int, hidden: int, out_dim: int, dropout: float) -> nn.Module:
    return nn.Sequential(
        nn.Linear(in_dim, hidden), nn.ReLU(), nn.Dropout(dropout),
        nn.Linear(hidden, out_dim), nn.ReLU())


class ScoreTwoTower(nn.Module):
    def __init__(self, apt_dim: int, prot_dim: int, cfg: dict):
        super().__init__()
        e = cfg["embed_dim"]
        self.apt = _tower(apt_dim, cfg["aptamer_hidden"], e, cfg["dropout"])
        self.prot = _tower(prot_dim, cfg["protein_hidden"], e, cfg["dropout"])
        self.head = nn.Sequential(
            nn.Linear(2 * e, cfg["head_hidden"]), nn.ReLU(),
            nn.Dropout(cfg["dropout"]), nn.Linear(cfg["head_hidden"], 1))

    def forward(self, x_apt, x_prot):
        z = torch.cat([self.apt(x_apt), self.prot(x_prot)], dim=1)
        return self.head(z).squeeze(1)


def _to(a, device, dtype=torch.float32):
    return torch.tensor(np.asarray(a), dtype=dtype, device=device)


def train_regression(X_apt, X_prot, y, cfg: dict, seed: int):
    _seed_everything(seed)
    device = "cuda" if torch.cuda.is_available() else "cpu"
    y = np.asarray(y, dtype=np.float32)
    y_mean, y_std = float(y.mean()), float(y.std() + 1e-8)
    yn = (y - y_mean) / y_std

    n = len(y)
    rng = np.random.default_rng(seed)
    idx = rng.permutation(n)
    n_val = max(1, int(cfg["val_fraction"] * n))
    val, tr = idx[:n_val], idx[n_val:]

    Xa = _to(X_apt, device); Xp = _to(X_prot, device); yt = _to(yn, device)
    model = ScoreTwoTower(X_apt.shape[1], X_prot.shape[1], cfg).to(device)
    opt = torch.optim.Adam(model.parameters(), lr=cfg["lr"])
    loss_fn = nn.MSELoss()
    best, best_state, bad = float("inf"), None, 0
    bs = cfg["batch_size"]
    for _ in range(cfg["max_epochs"]):
        model.train()
        perm = rng.permutation(len(tr))
        for i in range(0, len(tr), bs):
            b = tr[perm[i:i + bs]]
            opt.zero_grad()
            loss = loss_fn(model(Xa[b], Xp[b]), yt[b])
            loss.backward()
            opt.step()
        model.eval()
        with torch.no_grad():
            v = loss_fn(model(Xa[val], Xp[val]), yt[val]).item()
        if v < best - 1e-5:
            best, best_state, bad = v, {k: t.clone() for k, t in
                                        model.state_dict().items()}, 0
        else:
            bad += 1
            if bad >= cfg["patience"]:
                break
    if best_state is not None:
        model.load_state_dict(best_state)
    return model, {"y_mean": y_mean, "y_std": y_std}


def predict_scores(model, X_apt, X_prot) -> np.ndarray:
    device = next(model.parameters()).device
    model.eval()
    with torch.no_grad():
        out = model(_to(X_apt, device), _to(X_prot, device))
    return out.cpu().numpy()
```

- [ ] **Step 4: Запустить — пройдёт**

Run: `.venv/Scripts/python.exe -m pytest tests/test_ranking_twotower.py -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/models/ranking_twotower.py tests/test_ranking_twotower.py
git commit -m "Скор-two-tower и обучение регрессией (Часть 5)"
```

---

## Task 3: Ранговое обучение (BPR/маржинальная потеря + негативы)

**Files:** Modify `src/models/ranking_twotower.py`; Create `tests/test_train_ranking.py`.

**Interfaces:** `train_ranking(pos_apt, pos_prot, neg_pool, allowed, cfg, seed) ->
ScoreTwoTower` — `pos_apt (P,da)`, `pos_prot (P,dp)` — признаки позитивных пар;
`neg_pool (M,dp)` — пул мишеней-кандидатов в негативы; `allowed (P,M)` bool —
допустимые негативы для каждой позитивной пары; потеря `bpr` или `margin` по
`cfg["ranking_loss"]`, K = `cfg["n_negatives"]` негативов на позитив.

- [ ] **Step 1: Тест**

Create `tests/test_train_ranking.py`:
```python
import numpy as np
from src.models.ranking_twotower import train_ranking, predict_scores

CFG = {"aptamer_hidden": 32, "protein_hidden": 32, "embed_dim": 16,
       "head_hidden": 16, "dropout": 0.0, "lr": 0.01, "batch_size": 8,
       "max_epochs": 300, "patience": 300, "val_fraction": 0.25, "margin": 1.0,
       "n_negatives": 2, "ranking_loss": "bpr"}


def test_ranking_positive_scored_higher():
    rng = np.random.default_rng(0)
    da, dp = 4, 3
    # два аптамера, три мишени; апт i связывает мишень i (позитив)
    apt = rng.normal(size=(2, da)).astype("float32")
    tgt = rng.normal(size=(3, dp)).astype("float32")
    pos_apt = np.stack([apt[0], apt[1]]).astype("float32")     # P=2
    pos_prot = np.stack([tgt[0], tgt[1]]).astype("float32")
    neg_pool = tgt                                              # M=3
    allowed = np.array([[False, True, True],                   # апт0: негативы 1,2
                        [True, False, True]])                  # апт1: негативы 0,2
    model = train_ranking(pos_apt, pos_prot, neg_pool, allowed, CFG, seed=42)
    # у апт0 скор к своей мишени (tgt0) выше, чем к негативу tgt1
    s_pos = predict_scores(model, apt[0][None, :], tgt[0][None, :])[0]
    s_neg = predict_scores(model, apt[0][None, :], tgt[1][None, :])[0]
    assert s_pos > s_neg
```

- [ ] **Step 2: Запустить — упадёт**

Run: `.venv/Scripts/python.exe -m pytest tests/test_train_ranking.py -v`
Expected: FAIL (нет `train_ranking`).

- [ ] **Step 3: Дописать в `src/models/ranking_twotower.py`**

Добавить `import torch.nn.functional as F` вверху и функцию в конец файла:
```python
def train_ranking(pos_apt, pos_prot, neg_pool, allowed, cfg: dict, seed: int):
    """Ранговое обучение: для каждой позитивной пары (аптамер, мишень) сэмплируем
    K негативных мишеней из allowed и оптимизируем score(поз) > score(нег)."""
    _seed_everything(seed)
    device = "cuda" if torch.cuda.is_available() else "cpu"
    A = _to(pos_apt, device)               # (P, da)
    Ppos = _to(pos_prot, device)           # (P, dp)
    Pool = _to(neg_pool, device)           # (M, dp)
    P = A.shape[0]
    K = cfg["n_negatives"]
    allowed_idx = [np.where(row)[0] for row in np.asarray(allowed, dtype=bool)]

    rng = np.random.default_rng(seed)
    idx = rng.permutation(P)
    n_val = max(1, int(cfg["val_fraction"] * P))
    val, tr = idx[:n_val], idx[P and n_val:]

    model = ScoreTwoTower(pos_apt.shape[1], neg_pool.shape[1], cfg).to(device)
    opt = torch.optim.Adam(model.parameters(), lr=cfg["lr"])
    margin = cfg.get("margin", 1.0)
    use_bpr = cfg.get("ranking_loss", "bpr") == "bpr"
    bs = cfg["batch_size"]

    def sample_neg(rows):
        out = np.empty((len(rows), K), dtype=np.int64)
        for r, p in enumerate(rows):
            pool = allowed_idx[p]
            out[r] = rng.choice(pool, size=K,
                                replace=len(pool) < K) if len(pool) else 0
        return out

    def batch_loss(rows):
        neg = sample_neg(rows)                                  # (B, K)
        a = A[rows]                                             # (B, da)
        s_pos = model(a, Ppos[rows])                            # (B,)
        a_rep = a.unsqueeze(1).expand(-1, K, -1).reshape(-1, a.shape[1])
        n_rep = Pool[torch.as_tensor(neg.reshape(-1), device=device)]
        s_neg = model(a_rep, n_rep).reshape(len(rows), K)
        diff = s_pos.unsqueeze(1) - s_neg                       # (B, K)
        if use_bpr:
            return -F.logsigmoid(diff).mean()
        return F.relu(margin - diff).mean()

    best, best_state, bad = float("inf"), None, 0
    for _ in range(cfg["max_epochs"]):
        model.train()
        perm = rng.permutation(len(tr))
        for i in range(0, len(tr), bs):
            rows = tr[perm[i:i + bs]]
            opt.zero_grad()
            loss = batch_loss(rows)
            loss.backward()
            opt.step()
        model.eval()
        with torch.no_grad():
            v = batch_loss(val).item()
        if v < best - 1e-5:
            best, best_state, bad = v, {k: t.clone() for k, t in
                                        model.state_dict().items()}, 0
        else:
            bad += 1
            if bad >= cfg["patience"]:
                break
    if best_state is not None:
        model.load_state_dict(best_state)
    return model
```
Примечание: `tr = idx[P and n_val:]` — при P>0 это `idx[n_val:]`.

- [ ] **Step 4: Запустить — пройдёт**

Run: `.venv/Scripts/python.exe -m pytest tests/test_train_ranking.py -v`
Expected: PASS. Перед коммитом прогнать весь набор.

- [ ] **Step 5: Commit**

```bash
git add src/models/ranking_twotower.py tests/test_train_ranking.py
git commit -m "Ранговое обучение (BPR/маржинальная потеря, сэмплирование негативов)"
```

---

## Task 4: Оркестратор абляции 2×2 + боевой прогон

**Files:** Create `src/run_part5.py`, `tests/test_run_part5_smoke.py`.

**Interfaces:** `run_part5(cfg) -> pd.DataFrame` — таблица абляции (по ячейкам
encoding×objective: mean_roc_auc, mean_per_target_auc); пишет
`results/metrics/part5_spectrum.csv` и `part5_vs_olmpass.csv`.
`evaluate_cell(strength_fn, apt_feat_by_key, test_aptamers, panel, panel_emb,
known, precision_ks) -> tuple[float,float]` — средние per-aptamer ROC-AUC и
per-target AUC для одной обученной модели.

- [ ] **Step 1: Smoke-тест (синтетика)**

Create `tests/test_run_part5_smoke.py`:
```python
import numpy as np
from src.run_part5 import evaluate_cell


def test_evaluate_cell_returns_two_means():
    panel = ["P1", "P2", "P3"]
    panel_emb = {"P1": np.zeros(3, "float32"), "P2": np.ones(3, "float32"),
                 "P3": np.ones(3, "float32") * 2}
    test_aptamers = ["A1", "A2"]
    apt_feat = {"A1": np.array([1.0], "float32"), "A2": np.array([2.0], "float32")}
    known = {"A1": {"P1"}, "A2": {"P2"}}
    # сила = сумма признаков (детерминированно): апт×мишень
    def strength_fn(Xa, Xp):
        return Xa[:, 0] + Xp[:, 0]
    roc, pt = evaluate_cell(strength_fn, apt_feat, test_aptamers, panel,
                            panel_emb, known)
    assert 0.0 <= roc <= 1.0
    assert np.isnan(pt) or 0.0 <= pt <= 1.0
```

- [ ] **Step 2: Запустить — упадёт**

Run: `.venv/Scripts/python.exe -m pytest tests/test_run_part5_smoke.py -v`
Expected: FAIL.

- [ ] **Step 3: Реализовать оркестратор**

Create `src/run_part5.py`:
```python
"""Оркестратор Части 5: абляция 2×2 {регрессия|ранжирование} × {k-меры|Nucleotide
Transformer} на задаче спектра. Обучает скор-two-tower, строит профили
test-аптамеров по панели белков, считает per-aptamer ROC-AUC и per-target AUC,
сравнивает ячейки, baseline и OLMPASS IAP.

Запуск: .venv/Scripts/python.exe -m src.run_part5
"""
from itertools import product
from pathlib import Path

import numpy as np
import pandas as pd

from src.config import Part5Config, load_part5_config
from src.data.split import (build_pairs, assign_clusters, make_splits,
                            assert_no_cluster_leakage, check_split_balance)
from src.data.spectrum import build_target_panel, known_targets_by_aptamer
from src.data.olmpass import load_olmpass_iap
from src.features.aptamer import aptamer_features
from src.features.aptamer_lm import embed_aptamers
from src.features.protein_seqs import load_sequences
from src.features.protein import embed_sequences
from src.models.ranking_twotower import (train_regression, train_ranking,
                                         predict_scores)
from src.evaluation.ranking import ranking_metrics
from src.evaluation.per_target import per_target_auc

METRICS_DIR = Path("results/metrics")


def evaluate_cell(strength_fn, apt_feat, test_aptamers, panel, panel_emb, known,
                  precision_ks=(1, 5, 10)):
    """Профили test-аптамеров по панели; средние per-aptamer ROC-AUC и per-target
    AUC. strength_fn(X_apt, X_prot) -> сила связывания (больше = сильнее)."""
    panel_arr = np.array(panel)
    P = np.stack([panel_emb[t] for t in panel]).astype(np.float32)
    profiles = {}
    for a in test_aptamers:
        Xa = np.repeat(apt_feat[a][None, :], len(panel), axis=0).astype(np.float32)
        profiles[a] = np.asarray(strength_fn(Xa, P), dtype=float)
    # per-aptamer ROC-AUC
    aucs = []
    for a in test_aptamers:
        is_pos = np.isin(panel_arr, list(known.get(a, set())))
        if is_pos.sum() == 0:
            continue
        m = ranking_metrics(profiles[a], is_pos, ks=tuple(precision_ks))
        if not np.isnan(m["roc_auc"]):
            aucs.append(m["roc_auc"])
    # per-target AUC
    pt = []
    for j, tgt in enumerate(panel):
        strength = np.array([profiles[a][j] for a in test_aptamers])
        is_binder = np.array([tgt in known.get(a, set()) for a in test_aptamers])
        v = per_target_auc(strength, is_binder)
        if not np.isnan(v):
            pt.append(v)
    return (float(np.mean(aucs)) if aucs else float("nan"),
            float(np.mean(pt)) if pt else float("nan"))


def _apt_matrix(keys, feat):
    return np.stack([feat[k] for k in keys]).astype(np.float32)


def run_part5(cfg: Part5Config) -> pd.DataFrame:
    df = pd.read_parquet("data/dataset_protein.parquet")
    pairs = build_pairs(df, keep_qualified=True)
    pairs = assign_clusters(pairs, cfg.identity_threshold)
    pairs = make_splits(pairs, cfg.test_size, cfg.seed)
    assert_no_cluster_leakage(pairs)

    accs = sorted(pairs["target_key"].dropna().unique())
    seqs = load_sequences(accs, "data/uniprot_seqs.json")
    prot_emb = embed_sequences(seqs, cfg.esm_model, "data/esm2_emb.npz")

    kept = pairs[pairs.target_key.isin(prot_emb)].reset_index(drop=True)
    check_split_balance(kept["split_cluster"], cfg.test_size, "cluster")
    panel = build_target_panel(kept, prot_emb)
    known = known_targets_by_aptamer(kept)

    # --- признаки аптамеров: k-меры и NT (по уникальной последовательности) ---
    uniq = list(dict.fromkeys(zip(kept.aptamer_seq, kept.aptamer_type)))
    kmer_feat = {s: aptamer_features(s, t, cfg.kmer_k) for s, t in uniq}
    nt_raw = embed_aptamers({s: s for s, _ in uniq}, cfg.nt_model,
                            "data/nt_apt_emb.npz")
    nt_feat = {s: nt_raw[s] for s, _ in uniq}
    encodings = {"kmer": kmer_feat, "nt": nt_feat}

    train = kept[kept.split_cluster == "train"].reset_index(drop=True)
    test_aptamers = [s for s in dict.fromkeys(
        kept[kept.split_cluster == "test"].aptamer_seq)
        if known.get(s, set())]

    rows = []
    for enc_name, feat in encodings.items():
        # --- регрессия ---
        Xa = _apt_matrix(train.aptamer_seq, feat)
        Xp = np.stack([prot_emb[t] for t in train.target_key]).astype(np.float32)
        y = train["log_Kd"].to_numpy(np.float32)
        reg_model, _ = train_regression(Xa, Xp, y, cfg.nn, cfg.seed)
        reg_strength = lambda A, Pp, m=reg_model: -predict_scores(m, A, Pp)
        roc, pt = evaluate_cell(reg_strength, feat, test_aptamers, panel,
                                prot_emb, known)
        rows.append({"encoding": enc_name, "objective": "regression",
                     "mean_roc_auc": roc, "mean_per_target_auc": pt})

        # --- ранжирование ---
        train_targets = list(dict.fromkeys(train.target_key))
        tgt_index = {t: i for i, t in enumerate(train_targets)}
        neg_pool = np.stack([prot_emb[t] for t in train_targets]).astype(np.float32)
        pos_apt = _apt_matrix(train.aptamer_seq, feat)
        pos_prot = np.stack([prot_emb[t] for t in train.target_key]).astype(np.float32)
        apt_known_in_train = {s: {tgt_index[t] for t in known.get(s, set())
                                  if t in tgt_index} for s in train.aptamer_seq}
        allowed = np.ones((len(train), len(train_targets)), dtype=bool)
        for r, s in enumerate(train.aptamer_seq):
            allowed[r, list(apt_known_in_train[s])] = False        # не негативы
        rank_model = train_ranking(pos_apt, pos_prot, neg_pool, allowed,
                                   {**cfg.nn, "n_negatives": cfg.n_negatives,
                                    "ranking_loss": cfg.ranking_loss}, cfg.seed)
        rank_strength = lambda A, Pp, m=rank_model: predict_scores(m, A, Pp)
        roc, pt = evaluate_cell(rank_strength, feat, test_aptamers, panel,
                                prot_emb, known)
        rows.append({"encoding": enc_name, "objective": "ranking",
                     "mean_roc_auc": roc, "mean_per_target_auc": pt})

    spectrum = pd.DataFrame(rows)

    # --- сравнение с OLMPASS (per-target IAP на общих мишенях, лучшая ячейка) ---
    best = spectrum.sort_values("mean_per_target_auc", ascending=False).iloc[0]
    olm = load_olmpass_iap(cfg.best_models_dir)
    olm_agg = (olm.groupby("target_identifier")["iap"].mean().reset_index()
               .rename(columns={"iap": "olmpass_iap"}))
    vs = pd.DataFrame([{"best_cell": f"{best.encoding}+{best.objective}",
                        "our_mean_per_target_auc": best.mean_per_target_auc,
                        "olmpass_mean_iap": olm_agg["olmpass_iap"].mean(),
                        "n_olmpass_targets": len(olm_agg)}])

    METRICS_DIR.mkdir(parents=True, exist_ok=True)
    spectrum.to_csv(METRICS_DIR / "part5_spectrum.csv", index=False,
                    encoding="utf-8")
    vs.to_csv(METRICS_DIR / "part5_vs_olmpass.csv", index=False, encoding="utf-8")
    return spectrum


def main() -> None:
    cfg = load_part5_config("configs/part5.yaml")
    spectrum = run_part5(cfg)
    print("=== Абляция 2×2 (задача спектра) ===")
    print(spectrum.round(3).to_string(index=False))
    vs = pd.read_csv(METRICS_DIR / "part5_vs_olmpass.csv")
    print("\n=== Лучшая ячейка vs OLMPASS ===")
    print(vs.round(3).to_string(index=False))


if __name__ == "__main__":
    main()
```

- [ ] **Step 4: Запустить smoke-тест — пройдёт**

Run: `.venv/Scripts/python.exe -m pytest tests/test_run_part5_smoke.py -v`
Expected: PASS.

- [ ] **Step 5: Полный прогон (реальные данные, GPU)**

Run: `.venv/Scripts/python.exe -m src.run_part5`
Expected: печатается таблица абляции 2×2 (4 ячейки) и сравнение лучшей ячейки с
OLMPASS; создаются `results/metrics/part5_spectrum.csv`, `part5_vs_olmpass.csv`.
Первый прогон качает веса Nucleotide Transformer (100M) и считает NT-эмбеддинги
аптамеров (кэш); ESM-2 — из кэша. Медленные места — кластеризация (~несколько
минут), обучение 4 моделей.

**Длительность:** foreground-лимит ~10 мин — запусти прогон в ФОНЕ
(run_in_background у Bash), периодически проверяй `results/metrics/part5_spectrum.csv`
(наличие/размер, НЕ читай транскрипты). Когда готово — прочитай обе таблицы.
Если долго/падает — НЕ хакай, зафиксируй и верни DONE_WITH_CONCERNS/BLOCKED.

- [ ] **Step 6: Полный прогон тестов**

Run: `.venv/Scripts/python.exe -m pytest -q`
Expected: все зелёные.

- [ ] **Step 7: Commit**

```bash
git add src/run_part5.py tests/test_run_part5_smoke.py
git commit -m "Оркестратор Части 5: абляция 2×2 (цель × энкодер) на задаче спектра"
```

---

## Интерпретация (после прогона)

- **H9:** сравнить 4 ячейки по mean_roc_auc / mean_per_target_auc: помогает ли
  ранговая цель (ranking vs regression), помогает ли NT-энкодер (nt vs kmer),
  и их комбинация; насколько лучшая ячейка сокращает разрыв с OLMPASS IAP.
- Оговорки: приближённые «негативы» (unmeasured пары); разные протоколы оценки
  vs OLMPASS. Вывод — в текст диплома.

---

## Self-Review (проведена при написании плана)

- **Покрытие спецификации:** NT-эмбеддинги → Task 1; скор-модель+регрессия →
  Task 2; ранговое обучение → Task 3; абляция 2×2 + прогон → Task 4;
  окружение/конфиг → Task 0.
- **Плейсхолдеры:** отсутствуют; код полный. Честность (обучение и негативы на
  train, метрики на test) реализована фильтрами сплита в `run_part5`.
- **Согласованность типов:** `build_pairs(keep_qualified)` → split → spectrum →
  {aptamer_features | embed_aptamers} + ESM-2 → ScoreTwoTower ×
  {train_regression | train_ranking} → predict_scores → ranking_metrics /
  per_target_auc; olmpass.load_olmpass_iap → сравнение по target_identifier.
  Имена/формы согласованы; strength_fn возвращает силу (больше=сильнее) в обеих
  целях (регрессия: −предсказанный log-Kd; ранжирование: сырой скор).
```
