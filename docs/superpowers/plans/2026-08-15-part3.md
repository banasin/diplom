# План реализации: Часть 3 (спектр мишеней и кросс-реактивность)

> **Для исполнителя:** используйте `superpowers:subagent-driven-development`
> (рекомендуется) или `superpowers:executing-plans` для выполнения задача-за-
> задачей. Шаги помечены чекбоксами `- [ ]`.

**Goal:** показать кросс-реактивность как эмерджентное свойство парной модели
аффинности — предсказать матрицу аптамер×белок, извлечь профили и оценки
кросс-реактивности, проверить H5 (retrieval истинных мишеней) и H6 (мульти-
таргетные аптамеры предсказаны более кросс-реактивными), для XGBoost и two-tower.

**Architecture:** белковые пары → кластерный сплит → обучение XGBoost и two-tower
на train → панель белков + известные мишени по аптамеру → профили test-аптамеров
по панели → ранжирующие метрики (H5) и оценки кросс-реактивности с тестом
эмерджентности (H6).

**Tech Stack:** Python 3.10, numpy/pandas, scikit-learn, scipy, XGBoost, PyTorch,
ESM-2 (кэш), pytest.

## Global Constraints

- Windows, GPU NVIDIA RTX 4070 (CUDA). Интерпретатор `.venv/Scripts/python.exe`.
- **Никаких упоминаний внешних инструментов/ассистентов** в коде, комментариях и
  сообщениях коммитов. Коммиты **без** trailer-а `Co-Authored-By`. Автор — только
  пользователь.
- Данные, веса, эмбеддинги, результаты — **вне git** (см. `.gitignore`).
- Белковые пары: медиана log-Kd, оговорённые **оставлены** (как в Части 2).
- Кластеризация по аптамеру (U→T), сплит без утечки; профили — только для
  test-аптамеров (их пары не в обучении). Детерминизм при фиксированном seed.
- Знак: сильнее связывание = меньше log-Kd; «сила связывания» = −log_Kd
  (больше = сильнее). Ранжирующие метрики принимают ориентированный скор силы.
- Тесты запускаются из корня репозитория: `python -m pytest`.

---

## Структура файлов

| Файл | Ответственность |
|---|---|
| `configs/part3.yaml` | параметры Части 3 |
| `src/config.py` (расширение) | `Part3Config` + `load_part3_config` |
| `src/data/spectrum.py` | панель белков + известные мишени по аптамеру |
| `src/evaluation/ranking.py` | ROC-AUC/AP/precision@k/ранг |
| `src/cross_reactivity.py` | профили, оценка кросс-реактивности, тест эмерджентности |
| `src/run_part3.py` | оркестратор Части 3 |
| `tests/…` | юнит-тесты к каждому модулю |

Переиспускаем без изменений: `aptamer.py`, `protein.py`/`protein_seqs.py` (ESM-2),
`cluster.py`, `split.py` (`build_pairs`, `assign_clusters`, `make_splits`,
`assert_no_cluster_leakage`, `check_split_balance`), `baseline.py`, `twotower.py`,
`metrics.py`.

---

## Task 0: Конфиг Части 3

**Files:**
- Create: `configs/part3.yaml`
- Modify: `src/config.py`
- Create: `tests/test_config_part3.py`

**Interfaces:**
- Produces: `load_part3_config(path="configs/part3.yaml") -> Part3Config` с полями
  `seed:int, identity_threshold:float, test_size:float, kmer_k:int, esm_model:str,
  binding_log_kd:float, precision_ks:list, nn:dict, xgb:dict`.

- [ ] **Step 1: Тест**

Create `tests/test_config_part3.py`:
```python
from src.config import load_part3_config


def test_load_part3_config_defaults():
    cfg = load_part3_config("configs/part3.yaml")
    assert cfg.seed == 42
    assert cfg.identity_threshold == 0.95
    assert cfg.binding_log_kd == -7.0
    assert cfg.precision_ks == [1, 5, 10]
    assert isinstance(cfg.nn, dict) and isinstance(cfg.xgb, dict)
    # nn содержит ключи, нужные two-tower Части 1
    for key in ["aptamer_hidden", "protein_hidden", "embed_dim", "head_hidden",
                "dropout", "lr", "batch_size", "max_epochs", "patience",
                "val_fraction"]:
        assert key in cfg.nn
```

- [ ] **Step 2: Запустить — упадёт**

Run: `.venv/Scripts/python.exe -m pytest tests/test_config_part3.py -v`
Expected: FAIL.

- [ ] **Step 3: Конфиг**

Create `configs/part3.yaml`:
```yaml
seed: 42
identity_threshold: 0.95
test_size: 0.2
kmer_k: 4
esm_model: esm2_t33_650M_UR50D
binding_log_kd: -7.0          # порог «сильного связывания» (Kd ~100 нМ) для оценки кросс-реактивности
precision_ks: [1, 5, 10]
nn:
  aptamer_hidden: 256
  protein_hidden: 256
  embed_dim: 128
  head_hidden: 128
  dropout: 0.2
  lr: 0.001
  batch_size: 64
  max_epochs: 200
  patience: 20
  val_fraction: 0.15
xgb:
  n_estimators: 600
  max_depth: 6
  learning_rate: 0.05
  subsample: 0.8
  colsample_bytree: 0.8
```

- [ ] **Step 4: Расширить `src/config.py`**

Добавить в конец `src/config.py`:
```python
@dataclass
class Part3Config:
    seed: int
    identity_threshold: float
    test_size: float
    kmer_k: int
    esm_model: str
    binding_log_kd: float
    precision_ks: list
    nn: dict
    xgb: dict


def load_part3_config(path: str = "configs/part3.yaml") -> Part3Config:
    data = yaml.safe_load(Path(path).read_text(encoding="utf-8"))
    return Part3Config(
        seed=int(data["seed"]),
        identity_threshold=float(data["identity_threshold"]),
        test_size=float(data["test_size"]),
        kmer_k=int(data["kmer_k"]),
        esm_model=str(data["esm_model"]),
        binding_log_kd=float(data["binding_log_kd"]),
        precision_ks=list(data["precision_ks"]),
        nn=dict(data["nn"]),
        xgb=dict(data["xgb"]),
    )
```

- [ ] **Step 5: Запустить — пройдёт**

Run: `.venv/Scripts/python.exe -m pytest tests/test_config_part3.py -v`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add configs/part3.yaml src/config.py tests/test_config_part3.py
git commit -m "Конфиг Части 3 и его загрузчик"
```

---

## Task 1: Панель мишеней и известные мишени

**Files:**
- Create: `src/data/spectrum.py`
- Create: `tests/test_spectrum.py`

**Interfaces:**
- Produces:
  - `build_target_panel(pairs, protein_emb) -> list[str]` — упорядоченный список
    уникальных `target_key`, присутствующих в `protein_emb` (порядок первого
    появления).
  - `known_targets_by_aptamer(pairs) -> dict[str, set[str]]` — по `aptamer_seq`
    множество известных `target_key`.

- [ ] **Step 1: Тест**

Create `tests/test_spectrum.py`:
```python
import numpy as np
import pandas as pd
from src.data.spectrum import build_target_panel, known_targets_by_aptamer


def _pairs():
    return pd.DataFrame({
        "aptamer_seq": ["A1", "A1", "A2"],
        "target_key": ["P1", "P2", "P1"],
    })


def test_build_target_panel_filters_by_emb():
    emb = {"P1": np.zeros(4, np.float32)}         # P2 без эмбеддинга
    panel = build_target_panel(_pairs(), emb)
    assert panel == ["P1"]


def test_known_targets_by_aptamer():
    known = known_targets_by_aptamer(_pairs())
    assert known["A1"] == {"P1", "P2"}
    assert known["A2"] == {"P1"}
```

- [ ] **Step 2: Запустить — упадёт**

Run: `.venv/Scripts/python.exe -m pytest tests/test_spectrum.py -v`
Expected: FAIL.

- [ ] **Step 3: Реализовать**

Create `src/data/spectrum.py`:
```python
"""Панель белков-мишеней и множества известных мишеней по аптамеру для анализа
спектра мишеней и кросс-реактивности (Часть 3)."""


def build_target_panel(pairs, protein_emb) -> list:
    """Упорядоченный список уникальных белков-мишеней, у которых есть ESM-2
    эмбеддинг (панель P для профилей)."""
    seen = dict.fromkeys(pairs["target_key"])
    return [k for k in seen if k in protein_emb]


def known_targets_by_aptamer(pairs) -> dict:
    """Для каждого аптамера — множество его известных белков-мишеней."""
    out: dict = {}
    for seq, key in zip(pairs["aptamer_seq"], pairs["target_key"]):
        out.setdefault(seq, set()).add(key)
    return out
```

- [ ] **Step 4: Запустить — пройдёт**

Run: `.venv/Scripts/python.exe -m pytest tests/test_spectrum.py -v`
Expected: 2 passed.

- [ ] **Step 5: Commit**

```bash
git add src/data/spectrum.py tests/test_spectrum.py
git commit -m "Панель мишеней и известные мишени по аптамеру (спектр)"
```

---

## Task 2: Ранжирующие метрики

**Files:**
- Create: `src/evaluation/ranking.py`
- Create: `tests/test_ranking.py`

**Interfaces:**
- Produces: `ranking_metrics(strength, is_positive, ks=(1,5,10)) -> dict` с
  ключами `roc_auc, average_precision, precision@{k}..., best_true_rank`.
  `strength` — скор силы связывания (больше = сильнее); `is_positive` — булев
  вектор истинных мишеней. При отсутствии позитивов/негативов AUC/AP = `nan`.

- [ ] **Step 1: Тест**

Create `tests/test_ranking.py`:
```python
import numpy as np
from src.evaluation.ranking import ranking_metrics


def test_perfect_ranking():
    # позитивы имеют наибольшую силу
    strength = np.array([9.0, 8.0, 1.0, 0.5])
    is_pos = np.array([True, True, False, False])
    m = ranking_metrics(strength, is_pos, ks=(1, 2))
    assert abs(m["roc_auc"] - 1.0) < 1e-9
    assert m["precision@1"] == 1.0
    assert m["precision@2"] == 1.0
    assert m["best_true_rank"] == 1


def test_worst_ranking():
    strength = np.array([1.0, 0.5, 9.0, 8.0])
    is_pos = np.array([True, True, False, False])
    m = ranking_metrics(strength, is_pos, ks=(1,))
    assert abs(m["roc_auc"] - 0.0) < 1e-9
    assert m["precision@1"] == 0.0
    assert m["best_true_rank"] == 3         # лучшая истинная — третья по силе


def test_no_positives_returns_nan():
    m = ranking_metrics(np.array([1.0, 2.0]), np.array([False, False]))
    assert np.isnan(m["roc_auc"])
```

- [ ] **Step 2: Запустить — упадёт**

Run: `.venv/Scripts/python.exe -m pytest tests/test_ranking.py -v`
Expected: FAIL.

- [ ] **Step 3: Реализовать**

Create `src/evaluation/ranking.py`:
```python
"""Ранжирующие метрики для оценки retrieval истинных мишеней аптамера из
предсказанного профиля силы связывания (Часть 3, H5)."""
import numpy as np
from sklearn.metrics import average_precision_score, roc_auc_score


def ranking_metrics(strength, is_positive, ks=(1, 5, 10)) -> dict:
    s = np.asarray(strength, dtype=float)
    y = np.asarray(is_positive).astype(int)
    out: dict = {}
    if 0 < y.sum() < len(y):
        out["roc_auc"] = float(roc_auc_score(y, s))
        out["average_precision"] = float(average_precision_score(y, s))
    else:
        out["roc_auc"] = float("nan")
        out["average_precision"] = float("nan")

    order = np.argsort(-s)                    # по убыванию силы связывания
    ranked = y[order]
    for k in ks:
        out[f"precision@{k}"] = (float(ranked[:k].mean())
                                 if k <= len(y) else float("nan"))
    pos_ranks = np.where(ranked == 1)[0]
    out["best_true_rank"] = int(pos_ranks[0] + 1) if len(pos_ranks) else -1
    return out
```

- [ ] **Step 4: Запустить — пройдёт**

Run: `.venv/Scripts/python.exe -m pytest tests/test_ranking.py -v`
Expected: 3 passed.

- [ ] **Step 5: Commit**

```bash
git add src/evaluation/ranking.py tests/test_ranking.py
git commit -m "Ранжирующие метрики (ROC-AUC/AP/precision@k/ранг)"
```

---

## Task 3: Профили и оценка кросс-реактивности

**Files:**
- Create: `src/cross_reactivity.py`
- Create: `tests/test_cross_reactivity.py`

**Interfaces:**
- Produces:
  - `predict_profiles(pair_predict_fn, apt_feats, panel_emb) -> np.ndarray
    (N, P)` — матрица предсказанных log-Kd test-аптамер×панель. `pair_predict_fn(
    X_apt, X_prot) -> np.ndarray` предсказывает log-Kd для выровненных массивов.
  - `cross_reactivity_score(profile, binding_log_kd) -> dict` —
    `{"n_binders": int, "spread": float}`: число мишеней панели с предсказанным
    log-Kd ≤ порога (предсказанные связывания) и std профиля.
  - `emergence_test(scores_multi, scores_single) -> dict` —
    `{"u", "p_value", "rank_biserial", "median_multi", "median_single"}`:
    односторонний тест Манна–Уитни (мульти > специфичные) + размер эффекта.

- [ ] **Step 1: Тест**

Create `tests/test_cross_reactivity.py`:
```python
import numpy as np
from src.cross_reactivity import (predict_profiles, cross_reactivity_score,
                                  emergence_test)


def test_predict_profiles_shape_and_order():
    apt = np.array([[1.0], [2.0]], np.float32)     # N=2, ka=1
    panel = np.array([[10.0], [20.0], [30.0]], np.float32)  # P=3, D=1
    # pair_predict_fn: сумма первых признаков — детерминированная проверка формы/порядка
    def fn(Xa, Xp):
        return Xa[:, 0] + Xp[:, 0]
    prof = predict_profiles(fn, apt, panel)
    assert prof.shape == (2, 3)
    assert np.allclose(prof[0], [11.0, 21.0, 31.0])   # аптамер 0 × панель
    assert np.allclose(prof[1], [12.0, 22.0, 32.0])


def test_cross_reactivity_score_counts_binders():
    profile = np.array([-8.0, -7.5, -6.0, -5.0])       # порог -7 -> 2 связывания
    sc = cross_reactivity_score(profile, binding_log_kd=-7.0)
    assert sc["n_binders"] == 2
    assert sc["spread"] > 0


def test_emergence_test_detects_shift():
    multi = [5, 6, 7, 8, 6]
    single = [1, 2, 1, 0, 2]
    res = emergence_test(multi, single)
    assert res["p_value"] < 0.05
    assert res["rank_biserial"] > 0                    # мульти > специфичные
    assert res["median_multi"] > res["median_single"]
```

- [ ] **Step 2: Запустить — упадёт**

Run: `.venv/Scripts/python.exe -m pytest tests/test_cross_reactivity.py -v`
Expected: FAIL.

- [ ] **Step 3: Реализовать**

Create `src/cross_reactivity.py`:
```python
"""Профили аптамер×панель и оценка кросс-реактивности как эмерджентного свойства
парной модели аффинности (Часть 3)."""
import numpy as np
from scipy.stats import mannwhitneyu


def predict_profiles(pair_predict_fn, apt_feats, panel_emb) -> np.ndarray:
    """Матрица предсказанных log-Kd (N аптамеров × P мишеней панели). Строит
    декартово произведение признаков и предсказывает батчем."""
    apt_feats = np.asarray(apt_feats, dtype=np.float32)
    panel_emb = np.asarray(panel_emb, dtype=np.float32)
    n, p = len(apt_feats), len(panel_emb)
    if n == 0 or p == 0:
        return np.zeros((n, p), dtype=np.float32)
    Xa = np.repeat(apt_feats, p, axis=0)          # (N*P, ka)
    Xp = np.tile(panel_emb, (n, 1))               # (N*P, D)
    preds = np.asarray(pair_predict_fn(Xa, Xp), dtype=np.float32)
    return preds.reshape(n, p)


def cross_reactivity_score(profile, binding_log_kd: float) -> dict:
    profile = np.asarray(profile, dtype=float)
    n_binders = int((profile <= binding_log_kd).sum())    # предсказанные связывания
    return {"n_binders": n_binders, "spread": float(profile.std())}


def emergence_test(scores_multi, scores_single) -> dict:
    a = np.asarray(scores_multi, dtype=float)
    b = np.asarray(scores_single, dtype=float)
    u, p = mannwhitneyu(a, b, alternative="greater")
    n1, n2 = len(a), len(b)
    rank_biserial = 2.0 * u / (n1 * n2) - 1.0     # эффект: >0 => мульти сильнее
    return {"u": float(u), "p_value": float(p),
            "rank_biserial": float(rank_biserial),
            "median_multi": float(np.median(a)),
            "median_single": float(np.median(b))}
```

- [ ] **Step 4: Запустить — пройдёт**

Run: `.venv/Scripts/python.exe -m pytest tests/test_cross_reactivity.py -v`
Expected: 3 passed.

- [ ] **Step 5: Commit**

```bash
git add src/cross_reactivity.py tests/test_cross_reactivity.py
git commit -m "Профили и оценка кросс-реактивности (тест эмерджентности)"
```

---

## Task 4: Оркестратор Части 3 + боевой прогон

**Files:**
- Create: `src/run_part3.py`
- Create: `tests/test_run_part3_smoke.py`

**Interfaces:**
- Produces: `run_part3(cfg) -> tuple[pd.DataFrame, pd.DataFrame]` — таблицы
  ранжирования (H5) и кросс-реактивности (H6); пишет
  `results/metrics/part3_ranking.csv`, `results/metrics/part3_crossreact.csv`.
  `evaluate_retrieval(profiles, test_aptamers, panel, known, model_name,
  precision_ks) -> list[dict]` — H5 по каждому test-аптамеру.

- [ ] **Step 1: Smoke-тест (синтетика, без сети/моделей)**

Create `tests/test_run_part3_smoke.py`:
```python
import numpy as np
from src.run_part3 import evaluate_retrieval


def test_evaluate_retrieval_rows():
    panel = ["P1", "P2", "P3"]
    test_aptamers = ["A1", "A2"]
    known = {"A1": {"P1"}, "A2": {"P2", "P3"}}
    # профили: сила = -log_Kd; строим log_Kd так, что известные — сильнейшие
    profiles = np.array([[-9.0, -5.0, -4.0],      # A1: P1 сильнее всего (верно)
                         [-5.0, -9.0, -8.0]])     # A2: P2,P3 сильнее (верно)
    rows = evaluate_retrieval(profiles, test_aptamers, panel, known,
                              "xgboost", precision_ks=(1,))
    assert len(rows) == 2
    for r in rows:
        assert set(r) >= {"model", "aptamer", "roc_auc", "best_true_rank",
                          "n_known"}
    # у A1 идеальный ретрив: истинная мишень — ранг 1
    a1 = [r for r in rows if r["aptamer"] == "A1"][0]
    assert a1["best_true_rank"] == 1
```

- [ ] **Step 2: Запустить — упадёт**

Run: `.venv/Scripts/python.exe -m pytest tests/test_run_part3_smoke.py -v`
Expected: FAIL.

- [ ] **Step 3: Реализовать оркестратор**

Create `src/run_part3.py`:
```python
"""Оркестратор Части 3: кросс-реактивность как эмерджентное свойство парной
модели. Обучает XGBoost и two-tower на train, строит профили test-аптамеров по
панели белков, считает retrieval (H5) и эмерджентную кросс-реактивность (H6).

Запуск из корня: .venv/Scripts/python.exe -m src.run_part3
"""
from pathlib import Path

import numpy as np
import pandas as pd

from src.config import Part3Config, load_part3_config
from src.data.split import (build_pairs, assign_clusters, make_splits,
                            assert_no_cluster_leakage, check_split_balance)
from src.data.spectrum import build_target_panel, known_targets_by_aptamer
from src.features.aptamer import aptamer_features
from src.features.protein_seqs import load_sequences
from src.features.protein import embed_sequences
from src.models.baseline import train_baseline, predict_baseline
from src.models.twotower import train_twotower, predict_twotower
from src.cross_reactivity import (predict_profiles, cross_reactivity_score,
                                  emergence_test)
from src.evaluation.ranking import ranking_metrics

METRICS_DIR = Path("results/metrics")


def evaluate_retrieval(profiles, test_aptamers, panel, known, model_name,
                       precision_ks) -> list:
    """H5: для каждого test-аптамера ранжирование панели по силе связывания."""
    panel_arr = np.array(panel)
    rows = []
    for i, apt in enumerate(test_aptamers):
        pos_keys = known.get(apt, set())
        is_pos = np.isin(panel_arr, list(pos_keys))
        if is_pos.sum() == 0:
            continue
        strength = -profiles[i]                    # сила = −log_Kd
        m = ranking_metrics(strength, is_pos, ks=tuple(precision_ks))
        rows.append({"model": model_name, "aptamer": apt,
                     "n_known": int(is_pos.sum()), **m})
    return rows


def _xgb_predict_fn(model):
    return lambda Xa, Xp: predict_baseline(model, np.hstack([Xa, Xp]))


def _twotower_predict_fn(model, stats):
    return lambda Xa, Xp: predict_twotower(model, Xa, Xp, stats)


def run_part3(cfg: Part3Config):
    df = pd.read_parquet("data/dataset_protein.parquet")
    pairs = build_pairs(df, keep_qualified=True)
    pairs = assign_clusters(pairs, cfg.identity_threshold)
    pairs = make_splits(pairs, cfg.test_size, cfg.seed)
    assert_no_cluster_leakage(pairs)

    accs = sorted(pairs["target_key"].dropna().unique())
    seqs = load_sequences(accs, "data/uniprot_seqs.json")
    prot_emb = embed_sequences(seqs, cfg.esm_model, "data/esm2_emb.npz")

    panel = build_target_panel(pairs, prot_emb)
    known = known_targets_by_aptamer(pairs)
    panel_emb = np.stack([prot_emb[t] for t in panel]).astype(np.float32)

    # --- обучающие признаки (только пары с эмбеддингом мишени) ---
    train_pairs = pairs[(pairs.split_cluster == "train")
                        & (pairs.target_key.isin(prot_emb))].reset_index(drop=True)
    check_split_balance(pairs[pairs.target_key.isin(prot_emb)]["split_cluster"],
                        cfg.test_size, "cluster")
    Xa_tr = np.stack([aptamer_features(r.aptamer_seq, r.aptamer_type, cfg.kmer_k)
                      for r in train_pairs.itertuples()]).astype(np.float32)
    Xp_tr = np.stack([prot_emb[t] for t in train_pairs.target_key]).astype(np.float32)
    y_tr = train_pairs["log_Kd"].to_numpy(np.float32)

    xgb = train_baseline(np.hstack([Xa_tr, Xp_tr]), y_tr, cfg.xgb, cfg.seed)
    net, stats = train_twotower(Xa_tr, Xp_tr, y_tr, cfg.nn, cfg.seed)

    # --- test-аптамеры (уникальные, с ≥1 известной мишенью в панели) ---
    panel_set = set(panel)
    test_rows = pairs[pairs.split_cluster == "test"]
    test_aptamers = [a for a in dict.fromkeys(
        zip(test_rows.aptamer_seq, test_rows.aptamer_type))
        if known.get(a[0], set()) & panel_set]
    apt_seqs = [a[0] for a in test_aptamers]
    apt_types = [a[1] for a in test_aptamers]
    test_feats = np.stack([aptamer_features(s, t, cfg.kmer_k)
                           for s, t in zip(apt_seqs, apt_types)]).astype(np.float32)

    ranking_rows, cross_rows = [], []
    for model_name, pred_fn in [("xgboost", _xgb_predict_fn(xgb)),
                                ("twotower", _twotower_predict_fn(net, stats))]:
        profiles = predict_profiles(pred_fn, test_feats, panel_emb)   # (N, P)
        ranking_rows += evaluate_retrieval(profiles, apt_seqs, panel, known,
                                           model_name, cfg.precision_ks)
        # H6: оценки кросс-реактивности + тест эмерджентности
        scores = [cross_reactivity_score(profiles[i], cfg.binding_log_kd)["n_binders"]
                  for i in range(len(apt_seqs))]
        n_known = [len(known.get(s, set()) & panel_set) for s in apt_seqs]
        multi = [sc for sc, nk in zip(scores, n_known) if nk >= 2]
        single = [sc for sc, nk in zip(scores, n_known) if nk == 1]
        rec = {"model": model_name, "n_multi": len(multi), "n_single": len(single),
               "mean_score_multi": float(np.mean(multi)) if multi else float("nan"),
               "mean_score_single": float(np.mean(single)) if single else float("nan")}
        if len(multi) >= 1 and len(single) >= 1:
            rec.update(emergence_test(multi, single))
        cross_rows.append(rec)

    ranking = pd.DataFrame(ranking_rows)
    cross = pd.DataFrame(cross_rows)
    METRICS_DIR.mkdir(parents=True, exist_ok=True)
    ranking.to_csv(METRICS_DIR / "part3_ranking.csv", index=False, encoding="utf-8")
    cross.to_csv(METRICS_DIR / "part3_crossreact.csv", index=False, encoding="utf-8")
    return ranking, cross


def main() -> None:
    cfg = load_part3_config("configs/part3.yaml")
    ranking, cross = run_part3(cfg)
    print("=== H5 (retrieval), сводка по моделям ===")
    print(ranking.groupby("model")[["roc_auc", "average_precision",
          "best_true_rank"]].mean().to_string())
    print("\n=== H6 (кросс-реактивность) ===")
    print(cross.to_string(index=False))


if __name__ == "__main__":
    main()
```

- [ ] **Step 4: Запустить smoke-тест — пройдёт**

Run: `.venv/Scripts/python.exe -m pytest tests/test_run_part3_smoke.py -v`
Expected: PASS.

- [ ] **Step 5: Полный прогон (реальные данные, GPU)**

Run: `.venv/Scripts/python.exe -m src.run_part3`
Expected: печатается сводка H5 (средние ROC-AUC/AP/ранг по моделям) и таблица H6;
создаются `results/metrics/part3_ranking.csv` и `part3_crossreact.csv`.
Эмбеддинги белков (ESM-2 650M) — из кэша Частей 1–2; медленное место —
кластеризация аптамеров (~несколько минут).

**Длительность:** foreground-лимит ~10 мин — запусти прогон в ФОНЕ
(run_in_background у Bash), периодически проверяй появление
`results/metrics/part3_ranking.csv` (наличие/размер, НЕ читай транскрипты). Когда
готово — прочитай обе таблицы. Если прогон долгий/падает — НЕ хакай, зафиксируй
и верни DONE_WITH_CONCERNS/BLOCKED.

- [ ] **Step 6: Полный прогон тестов**

Run: `.venv/Scripts/python.exe -m pytest -q`
Expected: все зелёные.

- [ ] **Step 7: Commit**

```bash
git add src/run_part3.py tests/test_run_part3_smoke.py
git commit -m "Оркестратор Части 3: retrieval (H5) и эмерджентная кросс-реактивность (H6)"
```

---

## Интерпретация (Задача-аналитика, после прогона)

- **H5:** средние ROC-AUC / AP / ранг лучшей истинной мишени по test-аптамерам —
  выше случайного (AUC > 0.5)? Сравнить XGBoost vs two-tower.
- **H6:** для каждой модели — `p_value` и `rank_biserial` теста эмерджентности
  (мульти vs специфичные) + `mean_score_multi` vs `mean_score_single`. Учесть
  малое `n_multi` (оговорка о статистике). Сравнить модели.
Вывод занести в текст диплома.

---

## Self-Review (проведена при написании плана)

- **Покрытие спецификации:** панель/известные мишени → Task 1; ранжирующие
  метрики (H5) → Task 2; профили + оценка кросс-реактивности + тест эмерджентности
  (H6) → Task 3; оркестратор + прогон + интерпретация → Task 4; конфиг → Task 0.
- **Плейсхолдеры:** отсутствуют; код приведён полностью. Инвариант честности
  (профили только для test-аптамеров, их пары не в train) реализован фильтрами
  сплита в `run_part3`.
- **Согласованность типов:** `build_pairs(keep_qualified)` (Часть 2) →
  `assign_clusters`/`make_splits` → `build_target_panel`/`known_targets_by_aptamer`
  → `predict_profiles`(pair_predict_fn XGBoost/two-tower) → `ranking_metrics`/
  `cross_reactivity_score`/`emergence_test`. nn-конфиг содержит все ключи twotower
  Части 1 (без адаптера). Имена и формы согласованы.
```
